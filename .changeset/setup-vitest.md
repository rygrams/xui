---
---

Add Vitest testing framework to the monorepo

Set up Vitest as the testing framework for the monorepo with the following changes:
- Install Vitest and @vitest/ui as dev dependencies
- Create Vitest configuration files for root and packages
- Add test scripts to @xaui/colors and docs packages
- Add comprehensive test suite for @xaui/colors package with 13 passing tests
- Enable test coverage and UI mode via npm scripts

This is a development-only change and does not affect published packages.
